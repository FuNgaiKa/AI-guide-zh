# 维护与贡献说明

这个仓库会持续记录 AI 模型与开发工具的实践。为避免文章随产品更新后失去可信度，新增或修改内容时请遵循以下约定。

## 信息类型要分开

- **官方事实**：优先引用厂商官方文档，并注明核对日期。
- **第三方评测**：注明基准名称、版本、配置、数据来源、计算方式和局限。
- **个人体验**：使用“我的体感”“在当前工作流中”等表述，不写成普遍结论。

不要把 API 价格写成 ChatGPT 或 Codex 的订阅额度，也不要把不同 harness、不同版本或不同推理档位的成绩直接相加比较。

## 更新现有文章

1. 在文章开头更新“最后更新”日期。
2. 使用体验按日期新增小节；除非原文事实有误，否则尽量保留旧判断。
3. 价格、型号、产品能力等易变化信息必须重新核验来源。
4. 评测数字如果重新计算，需要同时检查原始数据和榜单说明。
5. 新结论要说明它来自官方资料、公开评测还是个人体验。

## 新增教程

- 文档放在 `docs/` 下，并按主题建立目录。
- 文件名使用简洁的小写英文和连字符，例如 `codex/task-workflow.md`。
- 标题先回答读者问题，开头说明适用范围和更新时间。
- README 只链接已经完成、可以独立阅读的文章。
- 命令、配置和截图中不得包含 token、账号、内部地址或其他敏感信息。

## 提交前自检

- Markdown 表格在 GitHub 中可正常渲染。
- 所有相对链接都有对应文件，外部链接指向原始来源。
- 数字、百分比和币种口径前后一致。
- 文中没有把主观体验包装成官方结论。
- `git diff --check` 没有空白错误。

## 提交信息建议

使用简短、可读的提交信息，例如：

- `docs: add Luna Sol Astra model guide`
- `docs: update benchmark data for 2026-09-14`
- `docs: clarify Codex quota disclaimer`
